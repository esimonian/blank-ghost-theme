<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
	<meta charset="utf-8">
	<meta http-equiv="x-ua-compatible" content="ie=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">

	<title>{{meta_title}}</title>
	<meta name="theme-color" content="#34495e">

	<link href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:400,900" rel="stylesheet">
	<link rel="stylesheet" type="text/css" href="{{asset "css/main.css"}}">

	{{ghost_head}}
</head>

<body class="{{body_class}}">
	<header class="site-header{{#if @blog.cover_image}} cover" style="background-image: url('{{@blog.cover_image}}'){{/if}}">
		<div class="container">
			<div class="brand">
				{{#is "index"}}<h1>{{/is}}
					<a href="{{@blog.url}}">
					{{#if @blog.logo}}
						<img src="{{@blog.logo}}" alt="{{@blog.title}}">
					{{else}}
						{{@blog.title}}
					{{/if}}
					</a>
				{{#is "index"}}</h1>{{/is}}

				<p>{{@blog.description}}</p>
			</div>

			{{navigation}}
		</div>
	</header>

	<main class="container">
		{{{body}}}
	</main>

	<footer class="site-footer">
		<div class="container">
			<p class="copyrights"><a href="{{@blog.url}}">{{@blog.title}}</a> &copy; {{date format="YYYY"}}</p>
			<p class="powered-by">Powered by <a href="https://ghost.org/">Ghost</a></p>
		</div>
	</footer>

	<script src="{{asset "js/dist/main.min.js"}}"></script>
	{{{block "scripts"}}}

	{{ghost_foot}}
</body>
</html>
